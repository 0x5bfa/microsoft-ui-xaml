<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<Project DefaultTargets="Build" ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

    <PropertyGroup>
        <AppAnalysisPath Condition="'$(AppAnalysisPath)' == ''">$(RuntimeToolsPath)\AppAnalysis</AppAnalysisPath>
        <AppAnalysisObjPath Condition="'$(AppAnalysisObjPath)' == ''">$(XamlBuildOutputRoot)tools\runtime\AppAnalysis</AppAnalysisObjPath>
        <ProjectPreprocessorDefinitions>$(ProjectPreprocessorDefinitions) UNICODE; _UNICODE; WIL_SUPPRESS_PRIVATE_API_USE; MIDL_NS_PREFIX;</ProjectPreprocessorDefinitions>
    </PropertyGroup>

    <!-- Lifted IXP's private/internal interfaces are marked with [experimental]. We need access to them. -->
    <PropertyGroup>
        <ProjectPreprocessorDefinitions>$(ProjectPreprocessorDefinitions) ENABLE_WINRT_EXPERIMENTAL_TYPES=1;</ProjectPreprocessorDefinitions>
    </PropertyGroup>

    <!-- Avoid polluting the global namespace with Started, Completed, Canceled, Error (from Microsoft-Windows-XAML-ETWEvents.h) -->
    <PropertyGroup>
        <ProjectPreprocessorDefinitions>$(ProjectPreprocessorDefinitions)_HIDE_GLOBAL_ASYNC_STATUS;</ProjectPreprocessorDefinitions>
    </PropertyGroup>

    <ItemDefinitionGroup>
        <ClCompile>
            <WarningLevel>Level3</WarningLevel>
            <ExceptionHandling>Sync</ExceptionHandling>
        </ClCompile>
    </ItemDefinitionGroup>

    <PropertyGroup>
        <ProjectPreprocessorDefinitions Condition="'$(Configuration)'=='Debug'">$(ProjectPreprocessorDefinitions) DEBUG;</ProjectPreprocessorDefinitions>
    </PropertyGroup>

    <Import Project="$(MSBuildThisFileDirectory)..\..\..\eng\xamlbuild\atl.props"/>
    <Import Project="$(MSBuildThisFileDirectory)..\..\..\eng\xamlbuild\use-public-sdk.props"/>

    <PropertyGroup>
        <ProjectIncludeDirectories>
            $(LiftedIXPIncludePaths);
            $(ExternalDependencies);
            $(AppAnalysisObjPath)\idl;
            $(AppAnalysisPath)\dll\;
            $(ProjectIncludeDirectories);
            $(AppAnalysisPath)\common\;
            $(AppAnalysisPath)\runtime\common\;
            $(AppAnalysisPath)\runtime\rules\;
            $(AppAnalysisPath)\runtime\framework\;
            $(AppAnalysisPath)\idl\;
            $(XcpPath)\components\base\inc;
            $(XcpPath)\inc;
        </ProjectIncludeDirectories>
        <TargetDestination>$(TestBinplaceDestinationPath)\AppAnalysis</TargetDestination>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Condition="'$(IsWinmdProject)'!='true'" Include="$(AppAnalysisPath)\idl\Microsoft.Diagnostics.AppAnalysis.vcxproj" Project="{e1f36d19-f82d-474a-be26-fea52da5b6e6}"/>
    </ItemGroup>
</Project>
