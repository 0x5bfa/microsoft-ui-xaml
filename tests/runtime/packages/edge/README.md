This folder is the temporary staging location for WebView2 Runtime installers
used to build the test-only Microsoft.UI.DCPP.Dependencies.Edge package.

Do not check installer binaries into the repo. The package nuspec consumes
versioned subfolders created by the WebView2 update workflow.
