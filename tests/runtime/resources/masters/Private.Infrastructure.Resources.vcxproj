<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->

<Project DefaultTargets="Build" ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

    <PropertyGroup Label="Globals">
        <ConfigurationType>DynamicLibrary</ConfigurationType>
        <ProjectGuid>{b0a4d3c3-c3cd-44a3-a762-dc0c9ff4e897}</ProjectGuid>
    </PropertyGroup>

    <Import Project="$(MSBuildThisFileDirectory)..\..\..\..\eng\xamlbuild\Xaml.Cpp.Props"/>
    <Import Project="$(XcpPath)\project-lift.props"/>
    <Import Project="$(ProjectRoot)eng\versioning\winui-version.props" />

    <!-- ******************************** File Header ******************************\ -->
    <!-- Copyright (c) Microsoft Corporation.  All rights reserved.                   -->
    <!--                                                                              -->
    <!-- Project specific SOURCES file.                                               -->
    <!--                                                                              -->
    <!-- ***************************************************************************/ -->
    <PropertyGroup>
        <ResourceOnlyDll>true</ResourceOnlyDll>
        <Mui>false</Mui>
        <VisualTestBaselinesDir>$(ProjectRoot)generated\tests\visualbaselines\</VisualTestBaselinesDir>
        <ProjectIncludeDirectories>$(ProjectIncludeDirectories);$(XcpPath)\inc;$(XamlBuildPath);</ProjectIncludeDirectories>
    </PropertyGroup>

    <ItemGroup>
        <GenerateTestResources Include="$(VisualTestBaselinesDir)*.xml"/>
        <GenerateTestResources Include="$(VisualTestBaselinesDir)*.png"/>
        <GenerateTestResources Include="$(VisualTestBaselinesDir)*.pdf"/>

        <ClInclude Include="TestResource.h"/>

        <ResourceCompile Include="Resources.rc"/>
    </ItemGroup>

    <!-- In cases where we're producing a release build, some masters can be different if there's a type
         that isn't in the release WinMD.  In those cases, we'll want to grab the masters from the release
         folder, not the base folder, to allow for differences between the two. -->
    <ItemGroup Condition="'$(MUXFinalRelease)' == 'true'">
        <GenerateReleaseTestResources Include="$(VisualTestBaselinesDir)release\*.xml"/>
        <GenerateReleaseTestResources Include="$(VisualTestBaselinesDir)release\*.png"/>
        <GenerateReleaseTestResources Include="$(VisualTestBaselinesDir)release\*.pdf"/>
        <GenerateTestResources Remove="@(GenerateReleaseTestResources -> '$(VisualTestBaselinesDir)%(Filename)%(Extension)')"/>
        <GenerateTestResources Include="@(GenerateReleaseTestResources)"/>
    </ItemGroup>

    <Import Project="$(XamlBuildSettingsPath)\GenerateTestResources.targets" />

    <Import Project="$(MSBuildThisFileDirectory)..\..\..\..\eng\xamlbuild\Microsoft.UI.Xaml.Build.targets" />
</Project>
